# プロジェクト規約(Claude Code 用メモリ)

このファイルは Claude Code がセッション開始時に必ず読み込む。ここに書いた制約は
「守らせたいルール」として毎回反映される。プロジェクトに合わせて書き換えて使うこと。

## このリポジトリの性格

- Windows 上の C / C++ 開発(Visual Studio + VS Code + Claude Code の混在環境)。
- ソースは **BOM付き UTF-8 + CRLF**(MSVC と日本語ソースの慣習)。

## 文字コード・改行を壊さないこと(最重要)

C/C++ のソース(`*.c *.cpp *.h *.hpp` 等)は次を厳守する:

- **文字コードは BOM付き UTF-8**。BOM を除去しない。UTF-8(BOMなし)や Shift_JIS に変換しない。
- **改行コードは CRLF**。LF に変換しない。
- ファイル全体を書き換える(Write)より、必要箇所だけ Edit で直す方が上記を崩しにくい。
- これらは `.editorconfig`(表示・保存)と `.gitattributes`(コミット時)で機械的にも守られる。
  勝手に別の値へ変更しない。

その他のファイル(`.md .json .yml` 等)は UTF-8(BOMなし)+ LF。

## 整形

- C/C++ の整形はリポジトリ直下の `.clang-format` に従う(独自スタイルを持ち込まない)。
- 整形の手動実行: `clang-format -i <file>`

## ビルド・テスト

<!-- プロジェクトごとに実際のコマンドを書く。例:
- ビルド: Visual Studio でソリューションをビルド、または `msbuild <name>.sln`
- 実行: F5 / Ctrl+F5
-->
(このテンプレートには未設定。プロジェクトのビルド/実行コマンドをここに書くこと。)

## 触らないもの

- 生成物・ビルド出力(`.gitignore` 参照)はコミットしない。
- `*.env` などの秘匿ファイルは読み書きしない。
